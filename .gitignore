# ── 빌드 산출물 ─────────────────────────────────────────────
bin/
obj/
*.user
*.suo
*.cache

# ── 편집기 / IDE ────────────────────────────────────────────
.vs/
.idea/
*.sln.docstates
*.userosscache

# ── 도구 기록 (개인 환경) ───────────────────────────────────
.claude/
.omc/

# ── 매크로 저장 파일 (개인 설정 + 로컬 이미지 경로가 들어감) ──
*.macros.json

# ── 이미지 템플릿 (본인 화면 캡처라 공유하지 않음) ──────────
images/
templates/

# 배포용 exe (깃에 넣지 않고 Releases 로 올린다 — 84MB)
dist/
publish/
release-notes.md
