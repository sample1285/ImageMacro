# 바뀐 내용

이 파일의 각 버전 항목이 그대로 [Releases](https://github.com/sample1285/ImageMacro/releases) 설명으로 올라갑니다.

## v1.1.3

### 고친 것

- **매크로를 불러온 직후 스텝 카드의 오른쪽 글자가 잘리던 문제.**
  목록을 비운 직후에는 세로 스크롤바가 잠깐 사라져 카드 폭이 실제보다 넓게 잡혔고,
  카드를 다 붙인 뒤 스크롤바가 다시 생기면서 카드 오른쪽 끝이 그 밑으로 가려졌습니다.
  `꺼진 채 시작` 딱지가 여기 걸려 잘렸고, 다른 곳을 클릭해 목록을 다시 그리면
  제대로 된 폭이 나와서 "클릭하면 카드가 작아지며 글자가 멀쩡해지는" 것처럼 보였습니다.
  이제 카드 폭을 스크롤바와 상관없는 기준으로 잡습니다.
- `꺼진 채 시작` · `OFF` 딱지를 고정 좌표가 아니라 **잰 글자 폭**으로 붙입니다.
  글자가 길어져도 카드 밖으로 나가지 않습니다.

## v1.1.2

### 고친 것

- **오래 쓰다 보면 렉이 걸리고 스텝 목록이 아예 안 보이던 문제.**
  스텝 목록을 다시 그릴 때마다 창 자원(GDI·USER 핸들)과 글꼴이 회수되지 않고 쌓였습니다.
  윈도우 한 프로그램당 한도인 1만 개에 닿으면 카드를 더 만들지 못해 목록이 빈 채로 남았습니다.
  카드를 지울 때 확실히 정리하고 글꼴은 한 벌만 만들어 돌려쓰도록 고쳤습니다.
  400번 다시 그려도 자원이 늘지 않습니다.

### 빨라진 것

- 선택만 바뀔 때는 목록 전체를 다시 만들지 않고 그 카드만 다시 그립니다.
- 설정값을 고칠 때(글자 입력 등) 한 글자마다 목록을 다시 만들던 것을 모아서 한 번만 그립니다.
- 화면에 보이는 모습과 동작은 그대로입니다.

## v1.1.1

### 고친 것

- **스텝을 넣거나 지우면 번호로 가리키던 설정이 어긋나던 문제.**
  `끝나면 갈 스텝`, `끝난 뒤 지켜볼 스텝`, `묶음 번호`, `켜고 끌 스텝` 에 적어둔 번호가
  그대로 남아 매크로 흐름이 엉켰습니다.
  이제 스텝을 넣거나 지우거나 자리를 바꾸면 그 번호들도 함께 옮겨집니다.
  어떻게 옮겼는지는 상태줄에 알려줍니다.

## v1.1.0

### 새로 생긴 것

- **켜고 끄기 스텝** — 이미지가 보이면 다른 스텝들을 켜거나 끕니다.
  필요 없는 구간을 통째로 건너뛸 수 있습니다.
  동작은 `끄기` · `켜기` · `뒤집기` · `이것만 켜기` 중에 고릅니다.
- **꺼진 채 시작** — 스텝을 처음부터 꺼진 상태로 두고, 이미지가 감지됐을 때만 켜지게 합니다.
  꺼진 상태에서 시작해 필요한 구간만 켜지는 흐름을 만들 수 있습니다.

### 고친 것

- 묶음 가운데 스텝 하나가 꺼져 있으면 **묶음이 둘로 쪼개져** 따로 돌던 문제
- 묶음에서 목록에 먼저 적힌 이미지를 고르던 문제 — 이제 **가장 비슷한**(일치도가 높은) 것을 고릅니다.
  비슷하게 생긴 버튼이 섞여 있어도 제대로 골라냅니다.
- 정지한 직후 다시 시작하면 **이전 실행이 되살아나 두 개가 같이 돌던** 문제

## v1.0.0

첫 공개입니다. 화면에서 이미지를 찾아 클릭하는 윈도우용 매크로입니다.

### 할 수 있는 것

- **이미지가 뜰 때까지 기다렸다 클릭** — 로딩 시간이 들쭉날쭉해도 알아서 기다립니다
- **여러 이미지 중 먼저 뜨는 것만 클릭** — "성공" 또는 "실패" 처럼 갈라지는 상황 처리
- **조건 분기 · 반복** — 스텝 사이를 오가며 원하는 흐름을 만듭니다
- **다중 모니터** — 전체 화면 또는 특정 모니터만 골라서 검색 (세로로 돌린 모니터도 그대로 인식)
- **커서 없이 클릭** — 마우스 커서를 건드리지 않고 창에 클릭 신호만 보냅니다
- **ADB 클릭** — 블루스택 등 에뮬레이터에 adb로 터치를 보냅니다
  (커서 없이 클릭이 안 통하는 게임용)
- **실행 흐름 보기** — 만들어둔 매크로가 어떻게 흘러가는지 그림으로 확인

### 스텝 종류

이미지 찾아 클릭 · 여러 이미지 중 먼저 뜨는 것 클릭 · 키보드 입력 ·
마우스 이동과 클릭 · 시간 대기(밀리초 · 초 · 분) · 알림 띄우기
